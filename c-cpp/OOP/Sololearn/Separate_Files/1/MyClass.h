// The header file (.h) holds the function declarations (prototypes) and variable declarations.
// It currently includes a template for our new MyClass class, with one default constructor.

#pragma once 
// #ifndef MYCLASS_H
// #define MYCLASS_H

// pragma once is a non-standard but widely supported preprocessor directive designed to cause 
// the current source file to be included only once in a single compilation.
// Design to replace #ifndef and #define

class MyClass {
    public:
        MyClass();
    protected:
    private:
};

// #endif