#include <stdio.h>
#include <stdlib.h>

int main() {
    // Syntax of free()
    // free(ptr);
    // This statement frees the space allocated in the memory pointed by ptr.

    // freeing 2D memory
    //
    // for (i = 0; i < nX; i++){  
    //    free(array[i]);  
    // }     
    // free(array);
}